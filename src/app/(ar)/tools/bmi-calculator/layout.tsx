import type { Metadata } from "next";
export const metadata: Metadata = { title: "Bmi Calculator | أدواتك", description: "Bmi Calculator - أداة مجانية من أدواتك adwatak.cloud" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
