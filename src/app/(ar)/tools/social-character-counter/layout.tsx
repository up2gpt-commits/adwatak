import type { Metadata } from "next";
export const metadata: Metadata = { title: "Social Character Counter | أدواتك", description: "Social Character Counter - أداة مجانية من أدواتك adwatak.cloud" };
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
