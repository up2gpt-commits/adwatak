"use client";import { useState, useRef } from "react";import FAQSection from "../../../components/FAQSection";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";const faqs = [  { question: "Hangi görüntü formatları destekleniyor?", answer: "JPG (JPEG), PNG, BMP, WebP. Tüm yaygın görüntü formatları. Diğer formatlar (GIF, TIFF) yakında." },  { question: "Dönüştürme sırasında görüntü kalitesi düşer mi?", answer: "Hayır, orijinal görüntü kalitesi korunur. Çıktı PDF, görüntüleri orijinal çözünürlük ve renk derinliğinde içerir." },  { question: "Birden fazla görüntüyü tek bir PDF'e dönüştürebilir miyim?", answer: "Evet, birden fazla görüntü seçin — tek bir çok sayfalı PDF'te birleştirilirler. Her görüntü = bir sayfa." },  { question: "PDF'teki görüntülerin sırası nedir?", answer: "Seçtiğiniz sırayla. İlk seçilen görüntü = PDF'teki ilk sayfa." },  { question: "PDF'teki sayfa boyutunu ayarlayabilir miyim?", answer: "Şu anda her görüntü, orijinal en boy oranında bir sayfayı doldurur. Sayfa boyutu özelleştirme yakında." },  { question: "Yüksek çözünürlüklü görüntüleri destekliyor mu?", answer: "Evet, 20 megapiksele kadar görüntüler iyi çalışır. Daha büyük görüntülerin işlenmesi daha uzun sürebilir." },  { question: "Taranmış görüntüleri PDF'e dönüştürebilir miyim?", answer: "Evet, taranmış görüntüler (JPG, PNG) doğrudan PDF'e dönüştürülür. Kağıt belgeleri dijitalleştirmek için kullanışlıdır." },  { question: "Görüntüden PDF'e dönüştürmenin yaygın kullanım alanları nelerdir?", answer: "Taranmış kağıt belgeleri PDF'e dönüştürme, birden fazla görüntüden PDF oluşturma (sunum, portföy), görüntüleri standart bir formatta arşivleme, görüntü gruplarını tek bir dosya olarak gönderme." },];const relatedTools = [  { title: "PDF Birleştirici", icon: "📎", href: "/tr/tools/pdf-merger" },  { title: "PDF Bölücü", icon: "✂️", href: "/tr/tools/pdf-splitter" },  { title: "Renk Dönüştürücü", icon: "🎨", href: "/tr/tools/color-converter" },  { title: "Birim Dönüştürücü", icon: "📐", href: "/tr/tools/unit-converter" },  { title: "Fatura Oluşturucu", icon: "🧾", href: "/tr/tools/invoice-generator" },  { title: "Base64 Kodlayıcı", icon: "🔄", href: "/tr/tools/base64-encoder" },];const seoContent = [  "Görüntüleri (JPG, PNG, BMP, WebP) anında PDF belgelerine dönüştürün. Bir veya daha fazla görüntü seçin ve bunları tek bir PDF dosyasına dönüştürün. Tüm işlem tarayıcınızda gerçekleşir — hiçbir şey yüklenmez.",  "Şunlar için idealdir: taranmış belgeleri PDF'e dönüştürme, birden fazla görüntüden PDF oluşturma (sunumlar, portföyler), görüntüleri standart bir formatta arşivleme ve görüntü gruplarını tek bir dosya olarak gönderme.",  "İşlem %100 istemci tarafındadır — görüntüler cihazınızdan asla ayrılmaz. Hassas belgeler için tam gizlilik. Ücretsiz, 20MP'ye kadar görüntüleri destekler ve orijinal kaliteyi korur.",  "Görüntüleri sürükleyip bırakın veya seçmek için tıklayın. Küçük resimleri önizleyin, istenmeyen görüntüleri kaldırın ve tek tıklamayla PDF'inizi oluşturun.",];export default function Client() {  const [images, setImages] = useState<{ file: File; url: string; name: string }[]>([]);  const [processing, setProcessing] = useState(false);  const [progress, setProgress] = useState(0);  const hiddenInputRef = useRef<HTMLInputElement>(null);  const handleFiles = (files: FileList | null) => {    if (!files) return;    const newImages: { file: File; url: string; name: string }[] = [];    for (let i = 0; i < files.length; i++) {      const file = files[i];      if (file.type.startsWith("image/")) {        newImages.push({ file, url: URL.createObjectURL(file), name: file.name });      }    }    setImages((prev) => [...prev, ...newImages]);  };  const handleDrop = (e: React.DragEvent) => {    e.preventDefault();    handleFiles(e.dataTransfer.files);  };  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement>) => {    handleFiles(e.target.files);    e.target.value = "";  };  const removeImage = (index: number) => {    setImages((prev) => {      URL.revokeObjectURL(prev[index].url);      return prev.filter((_, i) => i !== index);    });  };  const convertToPdf = async () => {    if (images.length === 0) return;    setProcessing(true);    setProgress(0);    try {      const { default: jsPDF } = await import("jspdf");      const pdf = new jsPDF();      let firstPage = true;      for (let i = 0; i < images.length; i++) {        setProgress(Math.round((i / images.length) * 100));        const img = new Image();        const dataUrl = await new Promise<string>((resolve, reject) => {          img.onload = () => {            const canvas = document.createElement("canvas");            canvas.width = img.width;            canvas.height = img.height;            const ctx = canvas.getContext("2d")!;            ctx.drawImage(img, 0, 0);            resolve(canvas.toDataURL("image/jpeg", 0.95));          };          img.onerror = reject;          img.src = images[i].url;        });        if (!firstPage) {          pdf.addPage();        }        firstPage = false;        const pdfWidth = pdf.internal.pageSize.getWidth();        const pdfHeight = pdf.internal.pageSize.getHeight();        const imgWidth = img.width;        const imgHeight = img.height;        const ratio = Math.min(pdfWidth / imgWidth, pdfHeight / imgHeight);        const w = imgWidth * ratio;        const h = imgHeight * ratio;        const x = (pdfWidth - w) / 2;        const y = (pdfHeight - h) / 2;        pdf.addImage(dataUrl, "JPEG", x, y, w, h);      }      setProgress(100);      pdf.save("adwatak-images.pdf");    } catch (err) {      console.error("PDF generation error:", err);      alert("PDF oluşturulurken bir hata oluştu. Lütfen tekrar deneyin.");    } finally {      setProcessing(false);      setProgress(0);    }  };  const schemaName = "Görüntüden PDF'e";  const schemaDesc = "Görüntüleri çevrimiçi PDF'e dönüştürün — ücretsiz, tarayıcı tabanlı, yükleme yok";  const schemaCategory = "Yardımcı Araç";  const schemaUrl = "https://adwatak.cloud/tr/tools/image-to-pdf";  const breadcrumbItems = [    { name: "Ana Sayfa", url: "https://adwatak.cloud/tr" },    { name: "Dönüştürücüler", url: "https://adwatak.cloud/tr/category/converters" },    { name: "Görüntüden PDF'e", url: "https://adwatak.cloud/tr/tools/image-to-pdf" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, "tr", schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — adim adim kullanim */}
      <StructuredData data={howToSchema("Bu araci nasil kullanirim", "Tarayicinizda calisan ucretsiz arac. Kayit gerektirmez.", [{name:"Araci acin",text:"Adwatak sitesinde bu arac sayfasina gidin"},{name:"Verilerinizi girin",text:"Gerekli alanlari doldurun"},{name:"Sonuc alin",text:"Hesapla veya olustur butonuna tiklayin"},{name:"Kullanin veya paylasin",text:"Sonucu kopyalayin, indirin veya paylasin"}],"bir dakikadan az","tr")} />
      {/* GEO: Speakable — AI/ses motorlari */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Dönüştürücüler" categorySlug="converters" toolName="Görüntüden PDF'e" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">🖼️ Görüntüden PDF'e</h1>        <p className="text-sm text-gray-500 mb-6">Görüntüleri PDF'e dönüştürün — ücretsiz, gizli, tarayıcı tabanlı</p>        <div          onDrop={handleDrop}          onDragOver={(e) => e.preventDefault()}          onClick={() => hiddenInputRef.current?.click()}          className="bg-gray-50 rounded-xl p-10 text-center border-2 border-dashed border-gray-300 cursor-pointer hover:border-blue-400 transition-colors"        >          <p className="text-3xl mb-3">📸</p>          <p className="text-gray-500">Görüntüleri buraya sürükleyin veya seçmek için tıklayın</p>          <p className="text-xs text-gray-400 mt-2">JPG, PNG, BMP, WebP</p>          <input            ref={hiddenInputRef}            type="file"            accept="image/*"            multiple            onChange={handleInputChange}            className="hidden"          />        </div>        {images.length > 0 && (          <div className="mt-6">            <div className="flex flex-wrap gap-3 mb-4">              {images.map((img, i) => (                <div key={i} className="relative group">                  <img                    src={img.url}                    alt={img.name}                    className="w-20 h-20 object-cover rounded-lg border border-gray-200"                  />                  <button                    onClick={() => removeImage(i)}                    className="absolute -top-2 -right-2 bg-red-500 text-white w-6 h-6 rounded-full text-xs cursor-pointer border-none opacity-0 group-hover:opacity-100 transition-opacity"                  >                    ✕                  </button>                  <p className="text-[10px] text-gray-400 mt-1 text-center truncate max-w-20">{img.name.slice(0, 12)}</p>                </div>              ))}            </div>            <p className="text-sm text-gray-500 mb-3">{images.length} görüntü • kaldırmak için ✕ tıklayın</p>            <button              onClick={convertToPdf}              disabled={processing}              className="w-full bg-blue-600 hover:bg-blue-700 disabled:opacity-50 disabled:cursor-wait text-white font-bold py-3 px-6 rounded-xl transition-all cursor-pointer border-none text-base"            >              {processing                ? `⏳ PDF oluşturuluyor... %${progress}`                : `📄 ${images.length} görüntüyü PDF'e dönüştür`}            </button>            {processing && (              <div className="mt-3 w-full bg-gray-200 rounded-full h-2 overflow-hidden">                <div                  className="bg-blue-600 h-full rounded-full transition-all duration-300"                  style={{ width: `${progress}%` }}                />              </div>            )}          </div>        )}      </div>      <SEOContent content={seoContent} lang="tr" />      <FAQSection faqs={faqs} lang="tr" />      <RelatedTools tools={relatedTools} lang="tr" />      <ShareButtons lang="tr" />    </div>  );}