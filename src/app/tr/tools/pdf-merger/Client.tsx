"use client";import { useState, useRef } from "react";import FAQSection from "../../../components/FAQSection";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";const faqs = [  { question: "Bu aracı kullanırken dosyalarım güvende mi?", answer: "Evet, tüm işlemler tarayıcınızda (istemci tarafında) gerçekleşir. Hiçbir dosya sunucuya yüklenmez. Verileriniz cihazınızdan asla ayrılmaz." },  { question: "Maksimum dosya boyutu nedir?", answer: "Cihazınızın belleğine bağlıdır. 50MB altındaki dosyalar sorunsuz çalışır. Daha büyük dosyalar daha fazla zaman veya bellek gerektirebilir." },  { question: "Dosyaları belirli bir sırayla birleştirebilir miyim?", answer: "Evet, dosyaları istediğiniz sırayla seçin — aynı sırayla birleştirilecekler." },  { question: "PDF'leri birleştirmek ve eklemek arasındaki fark nedir?", answer: "Birleştirme, birden çok PDF dosyasını tek bir çok sayfalı belgede birleştirir. Aracımız tam dosya birleştirmeyi destekler." },  { question: "Parola korumalı PDF'leri destekliyor mu?", answer: "Hayır, korumalı dosyaların birleştirilmeden önce kilidinin açılması gerekir." },  { question: "Bir seferde kaç dosya birleştirebilirim?", answer: "Bir seferde en fazla 10 PDF dosyası." },  { question: "Birleştirilmiş PDF'in kalitesi nasıl?", answer: "Orijinal kalite korunur — sıkıştırma veya kalite kaybı olmaz." },  { question: "PDF'leri resimlerle birleştirebilir miyim?", answer: "Resimleri PDF'lerle birleştirmek için önce 'Resimden PDF'e' aracını kullanarak resimleri PDF'ye dönüştürün, ardından diğer PDF dosyalarıyla birleştirin." },];const relatedTools = [  { title: "PDF Bölücü", icon: "✂️", href: "/tr/tools/pdf-splitter" },  { title: "PDF Sıkıştırıcı", icon: "📦", href: "/tr/tools/pdf-compressor" },  { title: "Resimden PDF'e", icon: "🖼️", href: "/tr/tools/image-to-pdf" },  { title: "PDF'den Word'e", icon: "📝", href: "/tr/tools/pdf-to-word" },  { title: "Renk Dönüştürücü", icon: "🎨", href: "/tr/tools/color-converter" },  { title: "Birim Dönüştürücü", icon: "📐", href: "/tr/tools/unit-converter" },];const seoContent = [  "Birden çok PDF dosyasını anında tek bir belgede birleştirin. Tüm işlemler tarayıcınızda gerçekleşir — hiçbir şey yüklenmez. Ücretsiz, güvenli ve gizli.",  "Nasıl kullanılır: istediğiniz sırayla PDF dosyalarını seçin, görünmelerini bekleyin, ardından Birleştir'e tıklayın. Çıktı dosyası tüm sayfaları orijinal kalitede içerir.",  "Faturaları, sözleşmeleri, raporları ve taranmış belgeleri tek bir PDF dosyasında birleştirmek için mükemmeldir.",  "Tamamen ücretsiz, 10 dosyaya kadar destekler ve orijinal PDF kalitesini korur.",];export default function Client() {  const [files, setFiles] = useState<{ file: File; id: number }[]>([]);  const [processing, setProcessing] = useState(false);  const [progress, setProgress] = useState("");  const hiddenInputRef = useRef<HTMLInputElement>(null);  const idCounter = useRef(0);  const handleFiles = (newFiles: FileList | null) => {    if (!newFiles) return;    const pdfs: { file: File; id: number }[] = [];    for (let i = 0; i < newFiles.length; i++) {      if (newFiles[i].type === "application/pdf") {        pdfs.push({ file: newFiles[i], id: idCounter.current++ });      }    }    setFiles((prev) => [...prev, ...pdfs]);  };  const handleDrop = (e: React.DragEvent) => {    e.preventDefault();    handleFiles(e.dataTransfer.files);  };  const removeFile = (id: number) => {    setFiles((prev) => prev.filter((f) => f.id !== id));  };  const merge = async () => {    if (files.length < 2) return;    setProcessing(true);    try {      const { PDFDocument } = await import("pdf-lib");      const mergedPdf = await PDFDocument.create();      let total = files.length;      for (let i = 0; i < files.length; i++) {        setProgress(`Birleştiriliyor ${i + 1}/${total}...`);        const fileBytes = await files[i].file.arrayBuffer();        const pdf = await PDFDocument.load(fileBytes, { ignoreEncryption: true });        const pages = await mergedPdf.copyPages(pdf, pdf.getPageIndices());        pages.forEach((page) => mergedPdf.addPage(page));      }      setProgress("Son dosya oluşturuluyor...");      const pdfBytes = await mergedPdf.save();      const blob = new Blob([pdfBytes.buffer as ArrayBuffer], { type: "application/pdf" });      const url = URL.createObjectURL(blob);      const a = document.createElement("a");      a.href = url;      a.download = "merged.pdf";      a.click();      URL.revokeObjectURL(url);      setProgress("");    } catch (err) {      console.error(err);      alert("Birleştirme sırasında bir hata oluştu. Tüm dosyaların geçerli PDF olduğundan emin olun.");    } finally {      setProcessing(false);    }  };  const schemaName = "PDF Birleştirici";  const schemaDesc = "Birden çok PDF dosyasını tek bir dosyada birleştirin — ücretsiz, istemci tarafında";  const schemaCategory = "Utility";  const schemaUrl = "https://adwatak.cloud/tr/tools/pdf-merger";  const breadcrumbItems = [    { name: "Ana Sayfa", url: "https://adwatak.cloud/tr" },    { name: "Dönüştürücüler", url: "https://adwatak.cloud/tr/category/converters" },    { name: "PDF Birleştirici", url: "https://adwatak.cloud/tr/tools/pdf-merger" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, "tr", schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — adim adim kullanim */}
      <StructuredData data={howToSchema("Bu araci nasil kullanirim", "Tarayicinizda calisan ucretsiz arac. Kayit gerektirmez.", [{name:"Araci acin",text:"Adwatak sitesinde bu arac sayfasina gidin"},{name:"Verilerinizi girin",text:"Gerekli alanlari doldurun"},{name:"Sonuc alin",text:"Hesapla veya olustur butonuna tiklayin"},{name:"Kullanin veya paylasin",text:"Sonucu kopyalayin, indirin veya paylasin"}],"bir dakikadan az","tr")} />
      {/* GEO: Speakable — AI/ses motorlari */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Dönüştürücüler" categorySlug="converters" toolName="PDF Birleştirici" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">📎 PDF Birleştirici</h1>        <p className="text-sm text-gray-500 mb-6">Birden çok PDF'i tek bir dosyada birleştirin — ücretsiz ve gizli</p>        <div          onDrop={handleDrop}          onDragOver={(e) => e.preventDefault()}          onClick={() => hiddenInputRef.current?.click()}          className="bg-gray-50 rounded-xl p-10 text-center border-2 border-dashed border-gray-300 cursor-pointer hover:border-blue-400 transition-colors"        >          <p className="text-3xl mb-3">📄</p>          <p className="text-gray-500 mb-4">PDF dosyalarını buraya sürükleyin veya seçmek için tıklayın</p>          <button            type="button"            className="bg-blue-600 hover:bg-blue-700 text-white font-bold px-6 py-3 rounded-xl border-none cursor-pointer transition-all"          >            Dosyaları Seç          </button>          <input ref={hiddenInputRef} type="file" accept=".pdf" multiple onChange={(e) => handleFiles(e.target.files)} className="hidden" />        </div>        {files.length > 0 && (          <div className="mt-6">            <div className="space-y-2 mb-4">              {files.map((f, i) => (                <div key={f.id} className="flex items-center justify-between bg-gray-50 p-3 rounded-xl">                  <span className="text-sm text-gray-700">                    {i + 1}. {f.file.name} ({(f.file.size / 1024).toFixed(0)} KB)                  </span>                  <button onClick={() => removeFile(f.id)} className="text-red-500 text-sm cursor-pointer border-none bg-transparent">✕</button>                </div>              ))}            </div>            <button              onClick={merge}              disabled={processing || files.length < 2}              className="w-full bg-blue-600 hover:bg-blue-700 disabled:opacity-50 disabled:cursor-wait text-white font-bold py-3 px-6 rounded-xl transition-all cursor-pointer border-none text-base"            >              {processing ? `⏳ ${progress}` : `📎 ${files.length} dosyayı birleştir`}            </button>          </div>        )}      </div>      <SEOContent content={seoContent} lang="tr" />      <FAQSection faqs={faqs} lang="tr" />      <RelatedTools tools={relatedTools} lang="tr" />      <ShareButtons lang="tr" />    </div>  );}