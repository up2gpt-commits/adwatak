"use client";import { useState, useRef } from "react";import FAQSection from "../../../components/FAQSection";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";const faqs = [  { question: "Sıkıştırma PDF kalitesini etkiler mi?", answer: "Evet, sıkıştırma dosya boyutunu kalite pahasına azaltır. Bir seviye seçin: Düşük (yüksek kalite) veya Yüksek (daha fazla sıkıştırma)." },  { question: "Maksimum dosya boyutu nedir?", answer: "Tarayıcıda 100 MB'a kadar. 50 MB altındaki dosyalar sorunsuz çalışır." },  { question: "Sıkıştırma sırasında dosyalarım güvende mi?", answer: "Evet, tüm işlemler tarayıcınızda gerçekleşir. Hiçbir dosya hiçbir yere yüklenmez. Verileriniz cihazınızdan asla ayrılmaz." },  { question: "PDF boyutunu ne kadar azaltabilirim?", answer: "Ortalama %20-50 azalma. Metin PDF = %10-20. Yüksek görüntülü PDF = %40-60." },  { question: "PDF sıkıştırmaya ne zaman ihtiyacım olur?", answer: "E-posta ekleri (25 MB sınırı), web sitesi yüklemeleri, depolama alanından tasarruf." },  { question: "Parola korumalı PDF'leri sıkıştırabilir miyim?", answer: "Hayır, önce kilidini açmanız gerekir." },];const relatedTools = [  { title: "PDF Birleştirici", icon: "📎", href: "/tr/tools/pdf-merger" },  { title: "PDF Bölücü", icon: "✂️", href: "/tr/tools/pdf-splitter" },  { title: "Resimden PDF'e", icon: "🖼️", href: "/tr/tools/image-to-pdf" },  { title: "PDF'den Word'e", icon: "📝", href: "/tr/tools/pdf-to-word" },  { title: "Renk Dönüştürücü", icon: "🎨", href: "/tr/tools/color-converter" },  { title: "Birim Dönüştürücü", icon: "📐", href: "/tr/tools/unit-converter" },];const seoContent = [  "Kabul edilebilir kaliteyi korurken dosya boyutunu azaltmak için PDF dosyalarını sıkıştırın. PDF'nizi bırakın, sıkıştırma seviyesini seçin — sıkıştırılmış dosya indirilmeye hazır. Hepsi tarayıcınızda.",  "Kullanım alanları: e-posta ile büyük dosyalar gönderme (50 MB → 15 MB), web sitesi yüklemeleri, depolama alanından tasarruf.",  "Ücretsiz, dosya yüklemeden tarayıcıda çalışır, tüm PDF türlerini destekler.",];export default function Client() {  const [file, setFile] = useState<File | null>(null);  const [quality, setQuality] = useState(70);  const [processing, setProcessing] = useState(false);  const inputRef = useRef<HTMLInputElement>(null);  const compress = async () => {    if (!file) return;    setProcessing(true);    try {      const { PDFDocument } = await import("pdf-lib");      const bytes = await file.arrayBuffer();      const pdf = await PDFDocument.load(bytes, { ignoreEncryption: true });      const compressedBytes = await pdf.save({        useObjectStreams: quality < 80,        addDefaultPage: false,        objectsPerTick: 50,      });      const blob = new Blob([compressedBytes.buffer as ArrayBuffer], { type: "application/pdf" });      const url = URL.createObjectURL(blob);      const a = document.createElement("a");      a.href = url;      a.download = `compressed-${file.name}`;      a.click();      URL.revokeObjectURL(url);    } catch {      alert("Bir hata oluştu. Dosyanın geçerli bir PDF olduğundan emin olun.");    } finally {      setProcessing(false);    }  };  const schemaName = "PDF Sıkıştırıcı";  const schemaDesc = "PDF dosya boyutunu küçültün — ücretsiz, tarayıcınızda";  const schemaCategory = "Utility";  const schemaUrl = "https://adwatak.cloud/tr/tools/pdf-compressor";  const breadcrumbItems = [    { name: "Ana Sayfa", url: "https://adwatak.cloud/tr" },    { name: "Dönüştürücüler", url: "https://adwatak.cloud/tr/category/converters" },    { name: "PDF Sıkıştırıcı", url: "https://adwatak.cloud/tr/tools/pdf-compressor" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, "tr", schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — adim adim kullanim */}
      <StructuredData data={howToSchema("Bu araci nasil kullanirim", "Tarayicinizda calisan ucretsiz arac. Kayit gerektirmez.", [{name:"Araci acin",text:"Adwatak sitesinde bu arac sayfasina gidin"},{name:"Verilerinizi girin",text:"Gerekli alanlari doldurun"},{name:"Sonuc alin",text:"Hesapla veya olustur butonuna tiklayin"},{name:"Kullanin veya paylasin",text:"Sonucu kopyalayin, indirin veya paylasin"}],"bir dakikadan az","tr")} />
      {/* GEO: Speakable — AI/ses motorlari */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Dönüştürücüler" categorySlug="converters" toolName="PDF Sıkıştırıcı" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">📦 PDF Sıkıştırıcı</h1>        <p className="text-sm text-gray-500 mb-6">PDF dosya boyutunu küçültün — ücretsiz ve gizli</p>        <div onClick={() => inputRef.current?.click()} className="bg-gray-50 rounded-xl p-10 text-center border-2 border-dashed border-gray-300 cursor-pointer hover:border-blue-400 transition-colors mb-4">          <p className="text-3xl mb-3">📦</p>          <p className="text-gray-500">PDF dosyası seçmek için tıklayın</p>          <input ref={inputRef} type="file" accept=".pdf" onChange={(e) => { const f = e.target.files?.[0]; if (f) setFile(f); }} className="hidden" />        </div>        {file && (          <div className="bg-green-50 rounded-xl p-4 border border-green-200 mb-4">            <p className="text-sm text-green-800">{file.name} — {(file.size / 1024).toFixed(0)} KB</p>          </div>        )}        {file && (          <>            <div className="mb-4">              <label className="block text-sm font-semibold text-gray-700 mb-1.5">Sıkıştırma seviyesi: {quality}%</label>              <input type="range" min={30} max={100} value={quality} onChange={(e) => setQuality(Number(e.target.value))} className="w-full accent-blue-600" />              <div className="flex justify-between text-xs text-gray-400 mt-1">                <span>Yüksek sıkıştırma (küçük dosya)</span>                <span>Orijinal kalite (büyük dosya)</span>              </div>            </div>            <button onClick={compress} disabled={processing} className="w-full bg-blue-600 hover:bg-blue-700 disabled:opacity-50 disabled:cursor-wait text-white font-bold py-3 px-6 rounded-xl transition-all cursor-pointer border-none text-base">              {processing ? "⏳ Sıkıştırılıyor..." : "📦 Sıkıştır"}            </button>          </>        )}      </div>      <SEOContent content={seoContent} lang="tr" />      <FAQSection faqs={faqs} lang="tr" />      <RelatedTools tools={relatedTools} lang="tr" />      <ShareButtons lang="tr" />    </div>  );}