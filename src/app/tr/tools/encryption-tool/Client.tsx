"use client"; import { useState } from "react"; import StructuredData, { toolSchema, faqSchema } from "../../../components/StructuredData"; import FAQSection from "../../../components/FAQSection"; import RelatedTools from "../../../components/RelatedTools"; import SEOContent from "../../../components/SEOContent"; import Breadcrumb from "../../../components/Breadcrumb"; import ShareButtons from "../../../components/ShareButtons"; const faqs = [ {question:"Şifreleme Aracı nedir?",answer:"Sezar Şifrelemesi, Base64 ve Ters Metin yöntemlerini kullanarak metin şifrelemek ve şifresini çözmek için ücretsiz bir araç."}, {question:"Güvenli mi?",answer:"Yalnızca temel/eğitim amaçlı şifreleme. Gerçek güvenlik için profesyonel araçlar kullanın."}, {question:"Sezar Şifrelemesi nedir?",answer:"Harfleri bir anahtar (1-25) kadar kaydıran basit bir şifreleme."}, {question:"Verilerim güvende mi?",answer:"Her şey tarayıcınızda çalışır. Hiçbir veri cihazınızdan ayrılmaz."}, ]; const relatedTools = [ {title:"Şifre Oluşturucu",icon:"🔑",href:"/tr/tools/password-generator"}, {title:"Hash Oluşturucu",icon:"#️⃣",href:"/tr/tools/hash-generator"}, {title:"Base64 Kodlayıcı",icon:"🔣",href:"/tr/tools/base64-encoder"}, ]; const seo = ["Ücretsiz Şifreleme Aracı — Sezar Şifrelemesi, Base64, Ters Metin. Anında şifreleyin ve şifresini çözün.", "Temel şifreleme ve eğitim amaçları için mükemmel."]; function caesar(text:string,shift:number,decode:boolean){ const s=decode?-shift:shift; return text.split("").map(c=>{ if(c>="a"&&c<="z")return String.fromCharCode(((c.charCodeAt(0)-97+s+26)%26)+97); if(c>="A"&&c<="Z")return String.fromCharCode(((c.charCodeAt(0)-65+s+26)%26)+65); return c; }).join(""); } export default function ClientEn(){ const [text,setText]=useState(""); const [mode,setMode]=useState("caesar"); const [key,setKey]=useState("3"); const [action,setAction]=useState("encrypt"); const [result,setResult]=useState(""); const process=()=>{ if(!text)return; if(mode==="caesar"){const s=parseInt(key)||3;setResult(caesar(text,s,action==="decrypt"));} else if(mode==="base64"){try{setResult(action==="encrypt"?btoa(text):atob(text));}catch{setResult("Hata: Geçersiz Base64 metni");}} else setResult(action==="encrypt"?text.split("").reverse().join(""):text.split("").reverse().join("")); }; return (<div className="max-w-[760px] mx-auto"> <StructuredData data={toolSchema("Şifreleme Aracı","Metni şifrele ve şifresini çöz","https://adwatak.cloud/tr/tools/encryption-tool","tr","Geliştirici Araçları")}/> <StructuredData data={faqSchema(faqs)}/> <Breadcrumb lang="tr" category="Geliştirici Araçları" categorySlug="tools" toolName="Şifreleme Aracı"/> <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6"> <h1 className="text-2xl font-extrabold mb-1">🔐 Şifreleme Aracı</h1> <p className="text-sm text-gray-500 mb-6">Metni birden çok yöntemle şifreleyin ve şifresini çözün</p> <div className="space-y-4"> <div className="grid grid-cols-3 gap-3"> <select value={mode} onChange={e=>setMode(e.target.value)} className="px-3 py-3 border border-gray-300 rounded-lg text-sm bg-white"> <option value="caesar">Sezar Şifrelemesi</option> <option value="base64">Base64</option> <option value="reverse">Ters Metin</option> </select> <select value={action} onChange={e=>setAction(e.target.value)} className="px-3 py-3 border border-gray-300 rounded-lg text-sm bg-white"> <option value="encrypt">Şifrele</option> <option value="decrypt">Şifre Çöz</option> </select> {mode==="caesar"&&<input type="number" value={key} onChange={e=>setKey(e.target.value)} min="1" max="25" className="px-3 py-3 border border-gray-300 rounded-lg text-sm" placeholder="Anahtar"/>} </div> <textarea value={text} onChange={e=>setText(e.target.value)} rows={4} placeholder="Metin girin..." className="w-full px-4 py-3 border border-gray-300 rounded-lg text-sm resize-none"/> <button onClick={process} className="w-full py-3 bg-gradient-to-r from-blue-600 to-blue-700 text-white font-bold rounded-lg hover:from-blue-700 transition-all shadow-md">İşle</button> {result&&<div className="bg-gray-50 rounded-xl p-4 border border-gray-200 text-sm whitespace-pre-wrap break-words max-h-40 overflow-y-auto">{result}</div>} </div> </div> <SEOContent content={seo} lang="tr"/> <FAQSection faqs={faqs} lang="tr"/> <RelatedTools tools={relatedTools} lang="tr"/> <ShareButtons lang="tr"/> </div>); }