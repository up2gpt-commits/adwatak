"use client";import { useState, useRef } from "react";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import FAQSection from "../../../components/FAQSection";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";const faqs = [  { question: "QR Kodunu bir görüntüden nasıl okurum?", answer: "QR Kodu içeren bir görüntü yükleyin — araç onu okur ve gizli metni veya URL'yi çıkarır. JPG, PNG, WEBP destekler. 4.296 alfanümerik karaktere kadar okuyabilir." },  { question: "Kameramla QR Kodlarını tarayabilir miyim?", answer: "Evet! Kamera moduna geçin ve kameranızı herhangi bir QR Koduna doğrultun. Hem mobil hem de masaüstü cihazlarda çalışır. QR Kodu otomatik olarak çözülecektir." },  { question: "QR Kodu taraması güvenli mi?", answer: "Adwatak QR Kodunu tarayıcınızda yerel olarak okur — hiçbir şey sunucuya gönderilmez. Ancak taradığınız bağlantılara dikkat edin. QR Kodları yalnızca depolamadır — hedef zararlı olabilir. Güvenlik için önce içeriği size gösteriyoruz." },  { question: "QR Tarayıcı ile QR Okuyucu arasındaki fark nedir?", answer: "Pratik bir fark yoktur. Her ikisi de QR Kodlarını çözer. QR Okuyucu = yüklenen görüntüleri okur. QR Tarayıcı = canlı kamera kullanır. Her ikisi de aynı şeyi yapar. Bazı uygulamalar bağlantıları otomatik açar — biz önce içeriği gösteririz." },  { question: "Hasarlı QR Kodlarını okuyabilir miyim?", answer: "QR Kodları %30'a kadar yerleşik hata düzeltme özelliğine sahiptir. Lekeli, çizik veya kısmen hasarlı kodlar hala okunabilir. Daha yüksek çözünürlüklü görüntüler daha iyi sonuç verir. Tamamen yok edilmiş kodlar kurtarılamaz." },  { question: "Bu hangi QR Kodu türlerini destekler?", answer: "Tüm standart türler: düz metin, URL'ler, Wi-Fi (SSID + Şifre), vCard kişiler, SMS, e-posta, telefon numaraları, coğrafi konum verileri ve uygulama mağazası bağlantıları." },  { question: "Bu araç ücretsiz mi?", answer: "Evet, %100 ücretsiz ve kayıt gerektirmez. Her şey tarayıcınızda gerçekleşir — görüntüler herhangi bir sunucuya yüklenmeden yerel olarak işlenir. Hızlı, gizli ve basit." },  { question: "QR Kodları için hangi baskı kalitesi gereklidir?", answer: "En az 300 DPI ile yazdırın. Minimum boyut: 2×2 cm (0,8×0,8 inç). Beklenen görüntüleme mesafesinden taramayı test edin. En iyi sonuçlar için yüksek kontrast (beyaz üzerine siyah) kullanın." },  { question: "QR Kodları video veya görüntü saklayabilir mi?", answer: "Hayır, QR Kodları yalnızca metin saklar. Ancak bir video veya görüntüye URL bağlantısı saklayabilirler. Maksimum depolama: ~3 KB. Basit URL'ler ve metin için bu fazlasıyla yeterlidir." },  { question: "QR Kodları vs NFC — gelecek ne olacak?", answer: "QR özel bir çip gerektirmez (her telefon kamerası okur), baskısı daha ucuzdur ve dağıtımı daha kolaydır. NFC daha hızlıdır ve dokunma gerektirir. Her ikisi de bir arada var olacak — baskı/reklam için QR, ödemeler için NFC." },];const relatedTools = [  { title: "QR Oluşturucu", icon: "🔳", href: "/tr/tools/qr-generator" },  { title: "WhatsApp Bağlantısı", icon: "💬", href: "/tr/tools/whatsapp-link" },  { title: "Base64 Kodlayıcı", icon: "🔄", href: "/tr/tools/base64-encoder" },  { title: "Şifre Oluşturucu", icon: "🔐", href: "/tr/tools/password-generator" },  { title: "Rastgele Sayı", icon: "🎲", href: "/tr/tools/random-number" },  { title: "SEO Denetimi", icon: "🔍", href: "/tr/tools/seo-audit" },];const seoContent = [  "QR Kod Okuyucu — Yüklenen görüntülerden veya kameranızdan QR Kodlarını anında çözün. Gizli metni, URL'yi veya herhangi bir veriyi çıkarmak için bir QR görüntüsü yükleyin veya kameranızı kullanın. Tamamen ücretsiz ve gizli — her şey tarayıcınızda çalışır.",  "Nasıl çalışır: Araç, QR Kodlarını algılamak ve çözmek için jsQR kütüphanesini kullanır. Görüntüyü tarayıcınızda yerel olarak işler, kare deseni algılar, veriyi çözer ve sonucu görüntüler. Hiçbir şey sunucuya yüklenmez.",  "QR Kodu kullanım alanları: Web sitesi bağlantıları, vCard kişiler, Wi-Fi otomatik bağlanma, restoran dijital menüleri, e-biletler, ödemeler, aşı kartları, reklamcılık ve ürün ambalajları. QR Kodları artık dünya çapında günlük yaşamın bir parçasıdır.",  "Güvenlik ipuçları: Rastgele QR Kodlarını taramayın — kaynağı doğrulayın. Halka açık yerlerdeki (istasyonlar, havaalanları) QR Kodları kurcalanmış olabilir (QRishing). Bağlantıyı açmadan önce önizleyin. İçeriği önce gösteren bir okuyucu kullanın (bizimki gibi).",  "Bu araç %100 ücretsizdir, indirme veya kayıt gerektirmez. QR Kodlarını hızlı ve güvenli bir şekilde okuyun. Pazarlamacılar, restoran sahipleri, etkinlik organizatörleri ve dijital bilet kullanan herkes için mükemmeldir."];export default function Client() {  const [result, setResult] = useState<string | null>(null);  const [mode, setMode] = useState<"upload" | "camera">("upload");  const [scanning, setScanning] = useState(false);  const videoRef = useRef<HTMLVideoElement>(null);  const streamRef = useRef<MediaStream | null>(null);  const handleImageUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {    const file = e.target.files?.[0];    if (!file) return;    setResult("QR Kodu Okunuyor...");    try {      const image = new Image();      const url = URL.createObjectURL(file);      image.src = url;      await new Promise((resolve) => { image.onload = resolve; });      const canvas = document.createElement("canvas");      canvas.width = image.width;      canvas.height = image.height;      const ctx = canvas.getContext("2d")!;      ctx.drawImage(image, 0, 0);      const imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);      const jsQR = (await import("jsqr")).default;      const code = jsQR(imageData.data, imageData.width, imageData.height);      if (code) {        setResult(code.data);      } else {        setResult("Görüntüde QR kodu bulunamadı. Daha net bir görüntü deneyin.");      }      URL.revokeObjectURL(url);    } catch {      setResult("Görüntü okunurken hata oluştu. Lütfen dosyayı kontrol edin.");    }  };  const startCamera = async () => {    setScanning(true);    try {      const stream = await navigator.mediaDevices.getUserMedia({ video: { facingMode: "environment" } });      streamRef.current = stream;      if (videoRef.current) {        videoRef.current.srcObject = stream;        videoRef.current.play();      }    } catch {      setResult("Kamera erişimi reddedildi. Tarayıcı izinlerinizi kontrol edin.");      setScanning(false);    }  };  const stopCamera = () => {    if (streamRef.current) {      streamRef.current.getTracks().forEach(t => t.stop());      streamRef.current = null;    }    setScanning(false);  };  const schemaName = "QR Kod Okuyucu";  const schemaDesc = `Çevrimiçi QR Kod Okuyucu - ücretsiz araç`;  const schemaCategory = "Araç";  const schemaUrl = "https://adwatak.cloud/tr/tools/qr-reader";  const breadcrumbItems = [    { name: "Ana Sayfa", url: "https://adwatak.cloud/tr" },    { name: "Oluşturucular", url: "https://adwatak.cloud/tr/category/calculators" },    { name: "QR Kod Okuyucu", url: "https://adwatak.cloud/tr/tools/qr-reader" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, 'tr', schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — adim adim kullanim */}
      <StructuredData data={howToSchema("Bu aracı nasıl kullanırım", "Tarayıcınızda çalışan ücretsiz araç. Kayıt gerektirmez.", [{name:"Aracı açın",text:"Adwatak sitesinde bu araç sayfasına gidin"},{name:"Verilerinizi girin",text:"Gerekli alanları doldurun"},{name:"Sonuç alın",text:"Hesapla veya oluştur butonuna tıklayın"},{name:"Kullanın veya paylaşın",text:"Sonucu kopyalayın, indirin veya paylaşın"}],"bir dakikadan az","tr")} />
      {/* GEO: Speakable — AI/ses motorlari */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Diğer Araçlar" categorySlug="daily" toolName="QR Kod Okuyucu" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">📷 QR Kod Okuyucu</h1>        <p className="text-sm text-gray-500 mb-6">QR kodlarını anında görüntüden veya kameranızdan okuyun</p>                <div className="flex gap-2 mb-4">          <button onClick={() => { setMode("upload"); stopCamera(); }} className={`px-4 py-2 rounded-full text-sm font-semibold cursor-pointer border-none ${mode === "upload" ? "bg-blue-600 text-white" : "bg-gray-100 text-gray-600"}`}>🖼 Görüntü Yükle</button>          <button onClick={() => { setMode("camera"); startCamera(); }} className={`px-4 py-2 rounded-full text-sm font-semibold cursor-pointer border-none ${mode === "camera" ? "bg-blue-600 text-white" : "bg-gray-100 text-gray-600"}`}>📸 Kamera</button>        </div>        {mode === "upload" && (          <div className="border-2 border-dashed border-gray-300 p-8 rounded-xl text-center">            <input type="file" accept="image/*" onChange={handleImageUpload} className="hidden" id="qr-upload-tr" />            <label htmlFor="qr-upload-tr" className="cursor-pointer">              <div className="text-4xl mb-3">🖼️</div>              <p className="text-blue-600 font-semibold">QR kod görüntüsü yüklemek için tıklayın</p>              <p className="text-sm text-gray-400 mt-1">JPG, PNG, WEBP</p>            </label>          </div>        )}        {mode === "camera" && (          <div className="text-center">            <video ref={videoRef} className="w-full max-w-[400px] mx-auto rounded-xl bg-black" playsInline />            {scanning && <p className="text-sm text-gray-400 mt-2">Kameranızı bir QR koduna doğrultun</p>}            <button onClick={stopCamera} className="bg-red-500 text-white font-bold p-2 rounded-xl border-none text-sm cursor-pointer mt-2">Kamerayı Durdur</button>          </div>        )}        {result && (          <div className="mt-6 p-4 bg-gray-50 rounded-xl">            <h3 className="font-bold text-sm mb-2">{result.startsWith("http") ? "🔗 URL:" : "📄 Metin:"}</h3>            <p className="text-sm text-gray-700 break-all bg-white p-3 rounded-lg border border-gray-200">{result}</p>            {result.startsWith("http") && (              <a href={result} target="_blank" rel="noopener noreferrer" className="inline-block mt-3 bg-blue-600 text-white font-bold p-2 rounded-xl text-sm no-underline">🔗 Bağlantıyı Aç</a>            )}          </div>        )}      </div>      <SEOContent content={seoContent} lang="tr" />      <FAQSection faqs={faqs} lang="tr" />      <RelatedTools tools={relatedTools} lang="tr" />      <ShareButtons lang="tr" />    </div>  );}