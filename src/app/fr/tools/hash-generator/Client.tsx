"use client";import { useState } from "react";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";import FAQSection from "../../../components/FAQSection";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";async function hash(input: string, algo: string) {  const encoder = new TextEncoder();  const data = encoder.encode(input);  const hashBuffer = await crypto.subtle.digest(algo, data);  return Array.from(new Uint8Array(hashBuffer)).map(b => b.toString(16).padStart(2, "0")).join("");}const faqs = [  { question: "What is a hash function?", answer: "A cryptographic hash function converts any input into a fixed-size string of characters. Even tiny changes produce completely different outputs. Hashing is one-way — you can't reverse a hash back to the original input." },  { question: "MD5 vs SHA-1 vs SHA-256 — what's the difference?", answer: "MD5 (128-bit): fast but broken — collisions found. SHA-1 (160-bit): deprecated — Google found a collision in 2017. SHA-256 (256-bit): secure, recommended. SHA-512 (512-bit): more secure, slower. Use SHA-256 for most purposes." },  { question: "Why use a hash generator?", answer: "Verify file integrity (compare checksums), store passwords securely (hash + salt), detect data tampering, create unique identifiers, digital signatures, blockchain transactions, and deduplication." },  { question: "Is hashing reversible?", answer: "No — hashing is one-way by design. Unlike encryption (which can be decrypted with a key), hashes cannot be reversed. That's why passwords are hashed — even if the database is stolen, passwords aren't revealed." },  { question: "What is a hash collision?", answer: "When two different inputs produce the same hash. MD5 has practical collisions (two files, same MD5 but different content). SHA-256 has no known collisions. Given 2^128+1 inputs to a 128-bit hash, collisions are guaranteed by the pigeonhole principle." },  { question: "How do rainbow tables work?", answer: "Precomputed tables of hash → plaintext mappings. If you hash 'password123' to 'abc123def', the rainbow table reverses it. Defense: salt (add random data before hashing). Salt makes rainbow tables useless because each salt is unique." },  { question: "What is salting?", answer: "Adding random data to input before hashing. Instead of hash('password'), use hash('password + randomSalt'). Each user gets a unique salt. Even if two users have the same password, their hashes differ. This prevents rainbow table attacks." },  { question: "What is SHA-256 used for?", answer: "SSL/TLS certificates, blockchain (Bitcoin uses SHA-256), file integrity verification (SHA-256 checksums), password storage (with PBKDF2/bcrypt/argon2), digital signatures, and software distribution verification." },  { question: "What is a checksum?", answer: "A hash used specifically for file integrity. Download a file, compute its SHA-256, compare with the official checksum. If they match, the file is authentic and uncorrupted. Software downloads and Linux ISOs publish checksums for verification." },  { question: "How long are different hashes?", answer: "MD5: 32 hex chars (128 bits). SHA-1: 40 hex chars (160 bits). SHA-256: 64 hex chars (256 bits). SHA-384: 96 hex chars (384 bits). SHA-512: 128 hex chars (512 bits). Longer = more secure but slower to compute." },  { question: "Can I use hashing for passwords?", answer: "Yes — but never use plain hash(password). Use a key derivation function: bcrypt (slow, salted), argon2 (memory-hard, modern), PBKDF2 (NIST standard), or scrypt (memory-hard). Our tool shows raw hashes — for passwords, pair with salt." },  { question: "What is HMAC?", answer: "Hash-based Message Authentication Code — a hash combined with a secret key. HMAC ensures both integrity (data not modified) and authenticity (from the right sender). HMAC-SHA-256 is used in JWT, API authentication, and TLS." },];const relatedTools = [  { title: "Base64 Encoder", icon: "🔐", href: "/fr/tools/base64-encoder" },  { title: "JSON Formatter", icon: "📋", href: "/fr/tools/json-formatter" },  { title: "Text Cleaner", icon: "🧹", href: "/fr/tools/text-cleaner" },  { title: "Password Generator", icon: "🔐", href: "/fr/tools/password-generator" },  { title: "Word Counter", icon: "📝", href: "/fr/tools/word-counter" },  { title: "QR Generator", icon: "📱", href: "/fr/tools/qr-generator" },];const seoContent = [  "Notre Hash Generator computes cryptographic hashes for any text input using MD5, SHA-1, SHA-256, SHA-384, and SHA-512. All computation happens in your browser — no data is ever sent to a server. Essential for developers, security professionals, and anyone verifying data integrity.",  "Example: The string 'Hello World' produces: MD5 = b10a8db16... (32 chars), SHA-1 = 0a4d55a8d... (40 chars), SHA-256 = a591a6d40... (64 chars), SHA-512 = 861844d67... (128 chars). Each algorithm produces a different length and value for the same input.",  "Use cases: (1) File integrity — verify downloaded files against published checksums. (2) Password storage — hash with salt (bcrypt/argon2 recommended over raw SHA). (3) Duplicate detection — hash content to find duplicates. (4) API development — verify request integrity with HMAC.",  "Security tips: MD5 and SHA-1 are cryptographically broken — don't use for security. SHA-256 is the minimum standard today. For passwords, use argon2, bcrypt, or PBKDF2 — not raw SHA-256. For file verification, SHA-256 is the industry standard (not MD5).",  "Related: Use with our Base64 Encoder for binary-to-text encoding. The Password Generator creates strong passwords to hash. The JSON Formatter structures data before hashing. The Text Cleaner normalizes input for consistent checksums.",  "All major operating systems include hashing tools: Linux/macOS: shasum -a 256 file, md5 file. Windows: certutil -hashfile file SHA256. Our browser tool provides the same functionality without the terminal — just paste and hash."];export default function Client() {  const [input, setInput] = useState("");  const [results, setResults] = useState<Record<string, string>>({});  const generate = async () => {    if (!input) return;    try {      const [md5, sha1, sha256, sha384, sha512] = await Promise.all([        hash(input, "MD5"), hash(input, "SHA-1"), hash(input, "SHA-256"), hash(input, "SHA-384"), hash(input, "SHA-512")      ]);      setResults({ MD5: md5, "SHA-1": sha1, "SHA-256": sha256, "SHA-384": sha384, "SHA-512": sha512 });    } catch { setResults({}); }  };  const schemaName = "#⃣ Hash Generator";const schemaDesc = `Online #⃣ Hash Generator - outil gratuit`;const schemaCategory = "Utility";const schemaUrl = "https://adwatak.cloud/fr/tools/hash-generator";const breadcrumbItems = [  { name: "Accueil", url: "https://adwatak.cloud/fr" },  { name: "Développement", url: "https://adwatak.cloud/fr/category/calculators" },  { name: "#⃣ Hash Generator", url: "https://adwatak.cloud/fr/tools/hash-generator" },];return (    <div className="max-w-[760px] mx-auto">        <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, 'fr', schemaCategory)} />        <StructuredData data={faqSchema(faqs)} />        <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — step by step for AI engines */}
      <StructuredData data={howToSchema("Comment utiliser cet outil", "Outil gratuit en ligne. Fonctionne dans votre navigateur. Aucune inscription requise.", [{name:"Ouvrir l'outil",text:"Navigate to this tool page on Adawatak"},{name:"Entrez vos données",text:"Remplissez les champs requis"},{name:"Obtenez les résultats",text:"Cliquez sur le bouton Calculer ou Générer"},{name:"Utilisez ou partagez",text:"Copiez, téléchargez ou partagez les résultats"}],"moins d'une minute","fr")} />
      {/* GEO: Speakable — AI/voice engines */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Développement" categorySlug="developer-tools" toolName="Hash Generator" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">#️⃣ Hash Generator</h1>        <p className="text-sm text-gray-500 mb-6">Générez des hash MD5, SHA-1, SHA-256</p>        <textarea value={input} onChange={(e) => setInput(e.target.value)} className="w-full p-3 border-2 border-gray-200 rounded-xl text-lg outline-none mb-4 min-h-[80px]" placeholder="Enter text to hash..." />        <button onClick={generate} className="bg-blue-600 text-white font-bold p-3 rounded-xl border-none text-lg w-full cursor-pointer">Generate Hashes</button>      </div>      {Object.keys(results).length > 0 && (        <div className="bg-gray-50 rounded-xl p-5 border border-gray-200 mb-6">          {Object.entries(results).map(([algo, val]) => (            <div key={algo} className="mb-3 last:mb-0">              <p className="text-xs text-gray-600 mb-1">{algo}</p>              <p className="text-sm font-mono bg-white p-2 rounded-lg border border-gray-200 break-all">{val}</p>            </div>          ))}        </div>      )}      <SEOContent content={seoContent} />      <FAQSection faqs={faqs} />      <RelatedTools tools={relatedTools} />    <ShareButtons lang="fr" />    </div>  );}