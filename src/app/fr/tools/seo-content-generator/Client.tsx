"use client";import { useState } from "react";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";import FAQSection from "../../../components/FAQSection";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";interface GeneratedArticle {  title: string;  metaDescription: string;  slug: string;  content: string;  keywords: string[];  schemaType: string;  wordCount: number;  angle: string;}const faqs = [  { question: "What is SEO Content Generator?", answer: "A free AI-powered tool that creates multiple unique SEO-optimized articles from a single keyword. Each article targets a different angle, making it easier to rank for the same keyword across multiple pages." },  { question: "How does it work?", answer: "Enter your target keyword, optionally add a case study or context, and the AI generates 5 unique articles — each with a different angle, title, meta description, full content, FAQ section, and schema markup suggestions." },  { question: "Why generate multiple articles for the same keyword?", answer: "Multiple articles let you target different search intents (informational, commercial, navigational) and rank on multiple positions in Google. This is a proven strategy used by top SEO professionals." },  { question: "Are the articles truly unique?", answer: "Yes — each article has a different angle, title, structure, and writing style. They're designed to rank separately without competing with each other." },  { question: "Does it support Arabic content?", answer: "Yes, this tool supports both Arabic and English content generation." },  { question: "What AI model is used?", answer: "We use Google Gemini 2.0 Flash (free tier) via OpenRouter for fast, high-quality content generation." },  { question: "Can I use the generated content on my website?", answer: "Absolutely — the content is yours to use. We recommend reviewing and personalizing it before publishing." },  { question: "How many articles can I generate at once?", answer: "Up to 5 articles per request, each with a unique angle." },  { question: "Does it include internal linking suggestions?", answer: "Yes, each article includes suggestions for internal links to relevant tools on adwatak.cloud." },  { question: "Is it free?", answer: "Yes, 100% free. No registration required." },];const relatedTools = [  { title: "SEO Audit", icon: "🔍", href: "/fr/tools/seo-audit" },  { title: "AI Content Detector", icon: "🤖", href: "/fr/tools/ai-content-detector" },  { title: "Grammar Checker", icon: "✍️", href: "/fr/tools/grammar-checker" },  { title: "Plagiarism Checker", icon: "🔎", href: "/fr/tools/plagiarism-checker" },  { title: "Paraphrasing Tool", icon: "🔄", href: "/fr/tools/paraphrasing-tool" },  { title: "Word Counter", icon: "🔢", href: "/fr/tools/word-counter" },];const seoContent = [  "Free SEO Content Generator — Create 5 unique, SEO-optimized articles from a single keyword. Each article targets a different angle for maximum Google visibility.",  "Powered by Google Gemini 2.0 Flash AI. Generates articles with proper SEO structure: title tags, meta descriptions, headings, internal links, FAQ sections, and schema markup. Completely free.",  "Perfect strategy for ranking multiple times on Google for the same keyword. Different angles = different search intents = multiple ranking positions.",  "Includes: Title optimization, meta descriptions, full HTML content, keyword placement, internal linking suggestions, FAQ generation, and schema markup recommendations.",  "Pro tip: Generate 5 articles, publish one per week, and interlink them. This creates a content cluster that signals topical authority to Google.",];const ANGLE_SUGGESTIONS = [  "Beginner's Guide",  "Advanced Tutorial",  "Comparison/Review",  "Problem/Solution",  "Case Study",  "Step-by-Step Tutorial",  "FAQ Style",  "Listicle",  "How-To Guide",  "Expert Analysis",];export default function Client() {  const [keyword, setKeyword] = useState("");  const [caseStudy, setCaseStudy] = useState("");  const [count, setCount] = useState(5);  const [lang, setLang] = useState<"fr" | "ar">("fr");  const [articles, setArticles] = useState<GeneratedArticle[]>([]);  const [loading, setLoading] = useState(false);  const [error, setError] = useState("");  const [expandedArticle, setExpandedArticle] = useState<number | null>(null);  const [copiedIdx, setCopiedIdx] = useState<number | null>(null);  const generate = async () => {    if (!keyword || keyword.length < 3) {      setError("Please enter a keyword (at least 3 characters)");      return;    }    setError("");    setArticles([]);    setLoading(true);    try {      const res = await fetch("/api/seo-content-generator", {        method: "POST",        headers: { "Content-Type": "application/json" },        body: JSON.stringify({ keyword, caseStudy, count, lang }),      });      const data = await res.json();      if (!res.ok) {        throw new Error(data.error || "Failed to generate content");      }      setArticles(data.articles || []);    } catch (err: any) {      setError(err.error || err.message || "Failed to generate content. Please try again.");    } finally {      setLoading(false);    }  };  const copyArticle = (idx: number) => {    const article = articles[idx];    if (!article) return;    const text = `# ${article.title}\n\nMeta: ${article.metaDescription}\n\n${article.content.replace(/<[^>]+>/g, "")}`;    navigator.clipboard.writeText(text);    setCopiedIdx(idx);    setTimeout(() => setCopiedIdx(null), 2000);  };  const schemaName = "SEO Content Generator";  const schemaDesc = "Generate 5 unique SEO articles from one keyword";  const schemaCategory = "SEO & AI Tools";  const schemaUrl = "https://adwatak.cloud/fr/tools/seo-content-generator";  const breadcrumbItems = [    { name: "Home", url: "https://adwatak.cloud/fr" },    { name: "SEO & AI Tools", url: "https://adwatak.cloud/fr/tools/seo" },    { name: "SEO Content Generator", url: "https://adwatak.cloud/fr/tools/seo-content-generator" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, "fr", schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — step by step for AI engines */}
      <StructuredData data={howToSchema("How to use this tool", "Free online tool. Works directly in your browser. No registration required.", [{name:"Open the tool",text:"Navigate to this tool page on Adawatak"},{name:"Enter your data",text:"Fill in the required fields"},{name:"Get results",text:"Click the calculate or generate button"},{name:"Use or share",text:"Copy, download, or share the results"}],"less than a minute","fr")} />
      {/* GEO: Speakable — AI/voice engines */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb lang="fr" category="Dev Tools" categorySlug="seo" toolName="SEO Content Generator" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">🚀 SEO Content Generator</h1>        <p className="text-sm text-gray-500 mb-6">Generate 5 unique SEO-optimized articles from a single keyword</p>        {/* Input Section */}        <div className="space-y-4 mb-6">          <div>            <label className="block text-sm font-semibold text-gray-700 mb-1.5">Target Keyword *</label>            <input              value={keyword}              onChange={(e) => setKeyword(e.target.value)}              className="w-full p-3 border-2 border-gray-200 rounded-xl text-base outline-none focus:border-blue-500"              placeholder="e.g., free online calculator"            />          </div>          <div>            <label className="block text-sm font-semibold text-gray-700 mb-1.5">Case Study / Context (Optional)</label>            <textarea              value={caseStudy}              onChange={(e) => setCaseStudy(e.target.value)}              className="w-full h-24 p-3 border-2 border-gray-200 rounded-xl text-sm outline-none resize-y focus:border-blue-500"              placeholder="Add specific context, case study details, or unique angles to include..."            />          </div>          <div className="flex gap-4">            <div className="flex-1">              <label className="block text-sm font-semibold text-gray-700 mb-1.5">Number of Articles</label>              <select                value={count}                onChange={(e) => setCount(Number(e.target.value))}                className="w-full p-3 border-2 border-gray-200 rounded-xl text-sm outline-none"              >                {[1, 2, 3, 4, 5].map((n) => (                  <option key={n} value={n}>{n} Article{n > 1 ? "s" : ""}</option>                ))}              </select>            </div>            <div className="flex-1">              <label className="block text-sm font-semibold text-gray-700 mb-1.5">Language</label>              <select                value={lang}                onChange={(e) => setLang(e.target.value as "fr" | "ar")}                className="w-full p-3 border-2 border-gray-200 rounded-xl text-sm outline-none"              >                <option value="fr">English</option>                <option value="ar">العربية</option>              </select>            </div>          </div>          {error && (            <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-xl text-sm">              ⚠️ {error}            </div>          )}          <button            onClick={generate}            disabled={loading}            className="w-full bg-blue-600 hover:bg-blue-700 disabled:bg-blue-400 text-white font-bold py-3 px-6 rounded-xl transition-all cursor-pointer border-none text-base"          >            {loading ? (              <span className="flex items-center justify-center gap-2">                <svg className="animate-spin h-5 w-5" viewBox="0 0 24 24">                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" fill="none" />                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" />                </svg>                Generating {count} articles... (this may take 30-60 seconds)              </span>            ) : (              `🚀 Generate ${count} Article${count > 1 ? "s" : ""}`            )}          </button>        </div>        {/* Results */}        {articles.length > 0 && (          <div className="border-t border-gray-200 pt-6">            <h2 className="text-xl font-bold mb-4">📄 Generated Articles ({articles.length})</h2>            <div className="space-y-4">              {articles.map((article, idx) => (                <div key={idx} className="border border-gray-200 rounded-xl overflow-hidden">                  <div                    className="flex justify-between items-center p-4 bg-gray-50 cursor-pointer hover:bg-gray-100 transition-colors"                    onClick={() => setExpandedArticle(expandedArticle === idx ? null : idx)}                  >                    <div className="flex-1 min-w-0">                      <div className="flex items-center gap-2 mb-1">                        <span className="bg-blue-600 text-white text-xs font-bold px-2 py-0.5 rounded-full">#{idx + 1}</span>                        {article.angle && (                          <span className="bg-purple-100 text-purple-700 text-xs font-semibold px-2 py-0.5 rounded-full">{article.angle}</span>                        )}                        <span className="bg-green-100 text-green-700 text-xs font-semibold px-2 py-0.5 rounded-full">{article.wordCount} words</span>                      </div>                      <h3 className="font-bold text-sm truncate">{article.title}</h3>                    </div>                    <div className="flex gap-2 ml-2">                      <button                        onClick={(e) => { e.stopPropagation(); copyArticle(idx); }}                        className="text-xs bg-white border border-gray-300 hover:bg-gray-50 px-3 py-1.5 rounded-lg cursor-pointer"                      >                        {copiedIdx === idx ? "✅ Copied!" : "📋 Copy"}                      </button>                      <span className="text-gray-400 text-lg">{expandedArticle === idx ? "▲" : "▼"}</span>                    </div>                  </div>                  {expandedArticle === idx && (                    <div className="p-4 border-t border-gray-200 space-y-3">                      <div>                        <p className="text-xs font-semibold text-gray-500 mb-1">Title Tag:</p>                        <p className="text-sm bg-blue-50 p-2 rounded-lg">{article.title}</p>                      </div>                      <div>                        <p className="text-xs font-semibold text-gray-500 mb-1">Meta Description:</p>                        <p className="text-sm bg-green-50 p-2 rounded-lg">{article.metaDescription}</p>                      </div>                      <div>                        <p className="text-xs font-semibold text-gray-500 mb-1">Slug: <code className="text-xs bg-gray-100 px-1 rounded">/{article.slug}</code></p>                      </div>                      {article.keywords?.length > 0 && (                        <div>                          <p className="text-xs font-semibold text-gray-500 mb-1">Keywords:</p>                          <div className="flex flex-wrap gap-1">                            {article.keywords.map((kw, ki) => (                              <span key={ki} className="bg-gray-100 text-gray-700 text-xs px-2 py-0.5 rounded-full">{kw}</span>                            ))}                          </div>                        </div>                      )}                      <div>                        <p className="text-xs font-semibold text-gray-500 mb-1">Content:</p>                        <div                          className="text-sm bg-white border border-gray-200 rounded-lg p-4 max-h-96 overflow-y-auto prose prose-sm max-w-none"                          dangerouslySetInnerHTML={{ __html: article.content }}                        />                      </div>                    </div>                  )}                </div>              ))}            </div>          </div>        )}      </div>      <SEOContent content={seoContent} lang="fr" />      <FAQSection faqs={faqs} lang="fr" />      <RelatedTools tools={relatedTools} lang="fr" />      <ShareButtons lang="fr" />    </div>  );}