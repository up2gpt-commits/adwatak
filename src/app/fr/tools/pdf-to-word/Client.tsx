"use client";import { useState, useRef } from "react";import FAQSection from "../../../components/FAQSection";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";const faqs = [  { question: "What is PDF to Word?", answer: "A free tool that extracts text from PDF files. Works directly in your browser without uploading files." },  { question: "Is it free?", answer: "Yes, 100% free. No registration, no limits, no ads." },  { question: "Do I need to upload files to your server?", answer: "No! Everything happens in your browser. Your data never leaves your device." },  { question: "Does it support Arabic and English?", answer: "Yes, it extracts both Arabic and English text." },  { question: "Does it preserve formatting?", answer: "Text is extracted without formatting (font, size, color). For simple text copying this is sufficient. For full formatting, use a professional PDF editor." },  { question: "Can I use it commercially?", answer: "Yes, the extracted text can be used for personal and commercial purposes." },  { question: "How many pages can be processed?", answer: "Depends on your device memory. Files up to 100 pages work smoothly." },  { question: "Are images extracted too?", answer: "No, only text. Images and tables are not extracted." },];const relatedTools = [  { title: "PDF Merger", icon: "📎", href: "/fr/tools/pdf-merger" },  { title: "PDF Splitter", icon: "✂️", href: "/fr/tools/pdf-splitter" },  { title: "PDF Compressor", icon: "📦", href: "/fr/tools/pdf-compressor" },  { title: "Image to PDF", icon: "🖼️", href: "/fr/tools/image-to-pdf" },  { title: "Color Converter", icon: "🎨", href: "/fr/tools/color-converter" },  { title: "Unit Converter", icon: "📐", href: "/fr/tools/unit-converter" },];const seoContent = [  "Extract text from PDF files for free. Upload a PDF and get the text ready to copy and paste into Word or any other editor. All processing happens in your browser.",  "Perfect for students, researchers, and professionals who need to extract text from PDFs for translation, editing, or analysis.",  "Free, fast, and privacy-respecting — all processing is done locally in your browser.",  "After extraction, copy the text and paste it into Word (Office or Google Docs) for formatting and editing.",];export default function Client() {  const [file, setFile] = useState<File | null>(null);  const [text, setText] = useState("");  const [processing, setProcessing] = useState(false);  const inputRef = useRef<HTMLInputElement>(null);  const extractText = async () => {    if (!file) return;    setProcessing(true);    setText("");    try {      const pdfjs = await import("pdfjs-dist");      pdfjs.GlobalWorkerOptions.workerSrc = "/pdfjs/pdf.worker.min.mjs";            const arrayBuffer = await file.arrayBuffer();      const pdf = await pdfjs.getDocument({ data: arrayBuffer }).promise;      let fullText = "";      for (let i = 1; i <= pdf.numPages; i++) {        const page = await pdf.getPage(i);        const content = await page.getTextContent();        const pageText = content.items.map((item: any) => item.str).join(" ");        fullText += pageText + "\n\n";      }      setText(fullText.trim());    } catch (err) {      console.error(err);      setText("Could not extract text. Make sure the PDF is not password-protected and contains text.");    } finally {      setProcessing(false);    }  };  const copyText = () => {    navigator.clipboard.writeText(text);    alert("Text copied!");  };  const schemaName = "PDF to Word";  const schemaDesc = "Extract text from PDF for free";  const schemaCategory = "Utility";  const schemaUrl = "https://adwatak.cloud/fr/tools/pdf-to-word";  const breadcrumbItems = [    { name: "Home", url: "https://adwatak.cloud/fr" },    { name: "Converters", url: "https://adwatak.cloud/fr/category/converters" },    { name: "PDF to Word", url: "https://adwatak.cloud/fr/tools/pdf-to-word" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, "fr", schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — step by step for AI engines */}
      <StructuredData data={howToSchema("How to use this tool", "Free online tool. Works directly in your browser. No registration required.", [{name:"Open the tool",text:"Navigate to this tool page on Adawatak"},{name:"Enter your data",text:"Fill in the required fields"},{name:"Get results",text:"Click the calculate or generate button"},{name:"Use or share",text:"Copy, download, or share the results"}],"less than a minute","fr")} />
      {/* GEO: Speakable — AI/voice engines */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Converters" categorySlug="converters" toolName="PDF to Word" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">📄 PDF to Word</h1>        <p className="text-sm text-gray-500 mb-6">Extract text from PDF — free, private, client-side</p>        <div onClick={() => inputRef.current?.click()} className="bg-gray-50 rounded-xl p-10 text-center border-2 border-dashed border-gray-300 cursor-pointer hover:border-blue-400 transition-colors mb-4">          <p className="text-3xl mb-3">📄</p>          <p className="text-gray-500">Click to select a PDF file</p>          <input ref={inputRef} type="file" accept=".pdf" onChange={(e) => { const f = e.target.files?.[0]; if (f) setFile(f); }} className="hidden" />        </div>        {file && (          <div className="bg-green-50 rounded-xl p-4 border border-green-200 mb-4">            <p className="text-sm text-green-800">{file.name}</p>          </div>        )}        {file && (          <button onClick={extractText} disabled={processing}            className="w-full bg-blue-600 hover:bg-blue-700 disabled:opacity-50 disabled:cursor-wait text-white font-bold py-3 px-6 rounded-xl transition-all cursor-pointer border-none text-base mb-4">            {processing ? "⏳ Extracting text..." : "📄 Extract Text"}          </button>        )}        {text && (          <div className="mt-4">            <div className="flex justify-between items-center mb-2">              <p className="text-sm font-semibold text-gray-700">Extracted Text</p>              <button onClick={copyText} className="bg-gray-200 hover:bg-gray-300 text-gray-700 font-bold py-1.5 px-4 rounded-xl text-sm transition-all cursor-pointer border-none">                📋 Copy              </button>            </div>            <textarea readOnly value={text}              className="w-full h-64 p-4 border-2 border-gray-200 rounded-xl text-sm font-sans outline-none resize-y"              dir="auto" />            <p className="text-xs text-gray-400 mt-2">Copy the text and paste it into Word for formatting and editing</p>          </div>        )}      </div>      <SEOContent content={seoContent} lang="fr" />      <FAQSection faqs={faqs} lang="fr" />      <RelatedTools tools={relatedTools} lang="fr" />      <ShareButtons lang="fr" />    </div>  );}