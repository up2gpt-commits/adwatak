"use client";import { useState } from "react";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import FAQSection from "../../../components/FAQSection";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";const faqs = [  { question: "How does the background removal work?", answer: "The tool uses an AI model specifically trained to detect people and products in images and separate them from the background. Processing happens entirely in your browser (client-side) — no image is uploaded to any server." },  { question: "Is this tool completely free?", answer: "Yes, 100% free with no registration required. All processing is client-side using WebAssembly technology. First use may take time to download the AI model (~40MB) — subsequent uses are faster thanks to browser caching." },  { question: "What image formats are supported?", answer: "JPG, PNG, WebP. Images up to 20 megapixels work well. Larger images may take longer to process. Best results with people and product images on clear backgrounds." },  { question: "Is the result same quality as the original?", answer: "Yes, the result preserves the original image resolution. The only difference: the background becomes transparent (PNG). Fine details (hair, fur) may be less precise — this is normal with current AI technology." },  { question: "Is it good for e-commerce product images?", answer: "Excellent for product photos! Remove the background and get a clean white background — required by Amazon, eBay, and major retailers. Uniform backgrounds increase professionalism and sales." },  { question: "How long does processing take?", answer: "First time: 30-60 seconds (model download). Subsequent times: 3-10 seconds depending on image size. Images under 1MP: 2-3 seconds. Over 10MP: 8-15 seconds." },  { question: "Can I use the results commercially?", answer: "Yes, the tool is free and results are yours to use commercially: product images, resumes, profiles, marketing designs, and more. All rights belong to you." },  { question: "What size is the AI model?", answer: "~40MB, downloaded once on first use and cached by the browser. Subsequent uses load from cache instantly." },];const relatedTools = [  { title: "Image Resizer", icon: "🖼️", href: "/fr/tools/image-resizer" },  { title: "Image Compressor", icon: "📦", href: "/fr/tools/image-compressor" },  { title: "Image to PDF", icon: "📄", href: "/fr/tools/image-to-pdf" },  { title: "Color Converter", icon: "🎨", href: "/fr/tools/color-converter" },  { title: "QR Generator", icon: "🔳", href: "/fr/tools/qr-generator" },  { title: "SEO Audit", icon: "🔍", href: "/fr/tools/seo-audit" },];const seoContent = [  "AI Background Remover — upload a person or product photo and remove the background instantly. Get a transparent PNG result. Free, browser-based, and private — your image never leaves your device.",  "How it works: AI detects the main subject (person, product, animal) and separates it from the background with high accuracy. Perfect for e-commerce, resumes, profiles, and marketing designs.",  "Perfect for: Amazon/eBay product photos, professional resume headshots, profile pictures, catalog images, and presentation materials. Products on white backgrounds sell 30% more.",  "Completely free, works offline after first use (model cached), supports JPG/PNG/WebP, and produces transparent PNG files at original resolution. No signup required."];export default function Client() {  const [originalImage, setOriginalImage] = useState<string | null>(null);  const [resultImage, setResultImage] = useState<string | null>(null);  const [processing, setProcessing] = useState(false);  const [error, setError] = useState("");  const handleImageUpload = (e: React.ChangeEvent<HTMLInputElement>) => {    const file = e.target.files?.[0];    if (!file) return;    setError(""); setResultImage(null);    const reader = new FileReader();    reader.onload = (ev) => setOriginalImage(ev.target?.result as string);    reader.readAsDataURL(file);  };  const removeBg = async () => {    if (!originalImage) return;    setProcessing(true); setError("");    try {      const { removeBackground } = await import("@imgly/background-removal");      const blob = await removeBackground(originalImage);      setResultImage(URL.createObjectURL(blob));    } catch {      setError("Error processing image. Try again or use a different image.");    } finally { setProcessing(false); }  };  const downloadResult = () => {    if (!resultImage) return;    const a = document.createElement("a");    a.href = resultImage; a.download = "adwatak-no-bg.png"; a.click();  };  const reset = () => { setOriginalImage(null); setResultImage(null); setError(""); };  const schemaName = "Suppression Arrière-plan";  const schemaDesc = "En ligne AI Background Remover  - outil gratuit";  const schemaCategory = "Utility";  const schemaUrl = "https://adwatak.cloud/fr/tools/background-remover";  const breadcrumbItems = [    { name: "Accueil", url: "https://adwatak.cloud/fr" },    { name: "Daily Tools", url: "https://adwatak.cloud/fr/tools/daily" },    { name: "Suppression Arrière-plan", url: "https://adwatak.cloud/fr/tools/background-remover" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, 'fr', schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — step by step for AI engines */}
      <StructuredData data={howToSchema("Comment utiliser cet outil", "Outil gratuit en ligne. Fonctionne dans votre navigateur. Aucune inscription requise.", [{name:"Ouvrir l'outil",text:"Navigate to this tool page on Adawatak"},{name:"Entrez vos données",text:"Remplissez les champs requis"},{name:"Obtenez les résultats",text:"Cliquez sur le bouton Calculer ou Générer"},{name:"Utilisez ou partagez",text:"Copiez, téléchargez ou partagez les résultats"}],"moins d'une minute","fr")} />
      {/* GEO: Speakable — AI/voice engines */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Autres" categorySlug="daily" toolName="Background Remover" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">🖼️ Suppression Arrière-plan</h1>        <p className="text-sm text-gray-500 mb-6">Supprimez l'arrière-plan des images</p>        {!originalImage && (          <div className="border-2 border-dashed border-gray-300 p-8 rounded-xl text-center">            <input type="file" accept="image/*" onChange={handleImageUpload} className="hidden" id="bg-upload-en" />            <label htmlFor="bg-upload-en" className="cursor-pointer">              <div className="text-5xl mb-3">🖼️</div>              <p className="text-blue-600 font-semibold">Click to upload an image</p>              <p className="text-sm text-gray-400 mt-1">JPG, PNG, WebP — first time may take ~40s</p>            </label>          </div>        )}        {originalImage && !resultImage && (          <div>            <img src={originalImage} alt="Original" className="max-w-full max-h-[300px] mx-auto rounded-xl mb-4" />            <button onClick={removeBg} disabled={processing}              className="bg-blue-600 text-white font-bold p-4 rounded-xl border-none text-lg w-full cursor-pointer disabled:opacity-50 disabled:cursor-wait">              {processing ? "⏳ Removing background..." : "✨ Remove Background"}            </button>            {processing && <p className="text-xs text-gray-400 text-center mt-2">First-time model download ~40MB — takes 30-60s</p>}            {error && <p className="text-red-500 text-sm text-center mt-2">{error}</p>}          </div>        )}        {resultImage && (          <div>            <div className="grid grid-cols-2 gap-4 mb-4">              <div className="text-center">                <p className="text-xs text-gray-500 mb-1">Original</p>                <img src={originalImage!} alt="Original" className="w-full rounded-xl border border-gray-200" />              </div>              <div className="text-center">                <p className="text-xs text-gray-500 mb-1">No Background</p>                <img src={resultImage} alt="Result" className="w-full rounded-xl border border-gray-200" style={{ backgroundImage: "repeating-conic-gradient(#e5e7eb 0% 25%, transparent 0% 50%)", backgroundSize: "20px 20px" }} />              </div>            </div>            <div className="flex gap-3">              <button onClick={downloadResult} className="flex-1 bg-green-600 text-white font-bold p-3 rounded-xl border-none text-base cursor-pointer">⬇ Download (PNG)</button>              <button onClick={reset} className="flex-1 bg-gray-200 text-gray-700 font-bold p-3 rounded-xl border-none text-base cursor-pointer">🔄 Another Image</button>            </div>          </div>        )}      </div>      <SEOContent content={seoContent} lang="fr" />      <FAQSection faqs={faqs} lang="fr" />      <RelatedTools tools={relatedTools} lang="fr" />      <ShareButtons lang="fr" />    </div>  );}