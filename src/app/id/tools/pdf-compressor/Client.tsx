"use client";import { useState, useRef } from "react";import FAQSection from "../../../components/FAQSection";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";const faqs = [
  { question: "Apakah kompresi mempengaruhi kualitas PDF?", answer: "Ya, kompresi mengurangi ukuran file dengan mengorbankan kualitas. Pilih level: Rendah (kualitas tinggi) atau Tinggi (kompresi lebih banyak)." },
  { question: "Berapa ukuran file maksimum?", answer: "Hingga 100MB di browser. File di bawah 50MB berjalan lancar." },
  { question: "Apakah file saya aman selama kompresi?", answer: "Ya, semua pemrosesan terjadi di browser Anda. Tidak ada file yang diunggah ke mana pun. Data Anda tidak pernah meninggalkan perangkat Anda." },
  { question: "Seberapa banyak saya bisa mengurangi ukuran PDF?", answer: "Rata-rata pengurangan 20-50%. PDF teks = 10-20%. PDF dengan banyak gambar = 40-60%." },
  { question: "Kapan saya perlu kompresi PDF?", answer: "Lampiran email (batas 25MB), unggahan situs web, menghemat ruang penyimpanan." },
  { question: "Bisakah saya mengompres PDF yang dilindungi kata sandi?", answer: "Tidak, Anda perlu membukanya terlebih dahulu." }
];const relatedTools = [  { title: "Penggabung PDF", icon: "📎", href: "/id/tools/pdf-merger" },  { title: "Pemisah PDF", icon: "✂️", href: "/id/tools/pdf-splitter" },  { title: "Gambar ke PDF", icon: "🖼️", href: "/id/tools/image-to-pdf" },  { title: "PDF ke Word", icon: "📝", href: "/id/tools/pdf-to-word" },  { title: "Konverter Warna", icon: "🎨", href: "/id/tools/color-converter" },  { title: "Konverter Satuan", icon: "📐", href: "/id/tools/unit-converter" },];const seoContent = [  "Kompres file PDF untuk mengurangi ukuran file sambil menjaga kualitas yang dapat diterima. Seret PDF Anda, pilih tingkat kompresi — file terkompresi siap diunduh. Semua di browser Anda.",  "Use cases: sending large files via email (50MB → 15MB), website uploads, saving storage space.",  "Gratis, bekerja di browser tanpa mengunggah file, mendukung semua jenis PDF.",];export default function Client() {  const [file, setFile] = useState<File | null>(null);  const [quality, setQuality] = useState(70);  const [processing, setProcessing] = useState(false);  const inputRef = useRef<HTMLInputElement>(null);  const compress = async () => {    if (!file) return;    setProcessing(true);    try {      const { PDFDocument } = await import("pdf-lib");      const bytes = await file.arrayBuffer();      const pdf = await PDFDocument.load(bytes, { ignoreEncryption: true });      const compressedBytes = await pdf.save({        useObjectStreams: quality < 80,        addDefaultPage: false,        objectsPerTick: 50,      });      const blob = new Blob([compressedBytes.buffer as ArrayBuffer], { type: "application/pdf" });      const url = URL.createObjectURL(blob);      const a = document.createElement("a");      a.href = url;      a.download = `compressed-${file.name}`;      a.click();      URL.revokeObjectURL(url);    } catch {      alert("An error occurred. Make sure the file is a valid PDF.");    } finally {      setProcessing(false);    }  };  const schemaName = "Kompresor PDF";  const schemaDesc = "Reduce PDF file size — free, in your browser";  const schemaCategory = "Utility";  const schemaUrl = "https://adwatak.cloud/id/tools/pdf-compressor";  const breadcrumbItems = [    { name: "Home", url: "https://adwatak.cloud/id" },    { name: "Konverter", url: "https://adwatak.cloud/id/category/converters" },    { name: "Kompresor PDF", url: "https://adwatak.cloud/id/tools/pdf-compressor" },  ];  return (    <div className="max-w-[760px] mx-auto">      <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, "en", schemaCategory)} />      <StructuredData data={faqSchema(faqs)} />      <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — step by step for AI engines */}
      <StructuredData data={howToSchema("Cara menggunakan alat ini", "Free online tool. Works directly in your browser. No registration required.", [{name:"Buka alat",text:"Arahkan ke halaman alat ini di Adawatak"},{name:"Masukkan data Anda",text:"Isi kolom yang diperlukan"},{name:"Dapatkan hasil",text:"Klik tombol hitung atau buat"},{name:"Gunakan atau bagikan",text:"Salin, unduh, atau bagikan hasilnya"}],"kurang dari satu menit","id")} />
      {/* GEO: Speakable — AI/voice engines */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Konverter" categorySlug="converters" toolName="Kompresor PDF" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">📦 Kompresor PDF</h1>        <p className="text-sm text-gray-500 mb-6">Reduce PDF file size — free & private</p>        <div onClick={() => inputRef.current?.click()} className="bg-gray-50 rounded-xl p-10 text-center border-2 border-dashed border-gray-300 cursor-pointer hover:border-blue-400 transition-colors mb-4">          <p className="text-3xl mb-3">📦</p>          <p className="text-gray-500">Click to select a PDF file</p>          <input ref={inputRef} type="file" accept=".pdf" onChange={(e) => { const f = e.target.files?.[0]; if (f) setFile(f); }} className="hidden" />        </div>        {file && (          <div className="bg-green-50 rounded-xl p-4 border border-green-200 mb-4">            <p className="text-sm text-green-800">{file.name} — {(file.size / 1024).toFixed(0)} KB</p>          </div>        )}        {file && (          <>            <div className="mb-4">              <label className="block text-sm font-semibold text-gray-700 mb-1.5">Tingkat kompresi: {quality}%</label>              <input type="range" min={30} max={100} value={quality} onChange={(e) => setQuality(Number(e.target.value))} className="w-full accent-blue-600" />              <div className="flex justify-between text-xs text-gray-400 mt-1">                <span>High compression (small file)</span>                <span>Original quality (large file)</span>              </div>            </div>            <button onClick={compress} disabled={processing} className="w-full bg-blue-600 hover:bg-blue-700 disabled:opacity-50 disabled:cursor-wait text-white font-bold py-3 px-6 rounded-xl transition-all cursor-pointer border-none text-base">{processing ? "⏳ Mengompresi..." : " Kompres"}</button>          </>        )}      </div>      <SEOContent content={seoContent} lang="id" />      <FAQSection faqs={faqs} lang="id" />      <RelatedTools tools={relatedTools} lang="id" />      <ShareButtons lang="id" />    </div>  );}