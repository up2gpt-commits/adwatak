"use client";import { useState } from "react";import StructuredData, { speakableSchema, toolSchema, faqSchema, breadcrumbSchema,howToSchema } from "../../../components/StructuredData";import Breadcrumb from "../../../components/Breadcrumb";import ShareButtons from "../../../components/ShareButtons";import FAQSection from "../../../components/FAQSection";import RelatedTools from "../../../components/RelatedTools";import SEOContent from "../../../components/SEOContent";const faqs = [  { question: "What is a random number generator?", answer: "Generates unpredictable numbers within a range. Used for lotteries, giveaways, statistical sampling, gaming, cryptography, and decision-making. A true RNG requires entropy from physical processes. Our generator uses cryptographically secure random values." },  { question: "Random vs pseudo-random?", answer: "True random: from physical entropy (radioactive decay, atmospheric noise). Pseudo-random: algorithm with a seed. Most computer randomizers are pseudo-random. Our generator uses crypto.getRandomValues — cryptographically secure (not just Math.random)." },  { question: "How random is Math.random()?", answer: "Not very — it's a pseudo-random algorithm (typically xorshift128+). Predictable if you know the seed. Fine for games and demos. NOT secure for passwords, lottery, or security. Use crypto.getRandomValues (our generator) for real randomness." },  { question: "What numbers can I generate?", answer: "Any integer range: 1-10 (simple dice), 1-100 (percentages), 1-1000 (giveaway entries), 1-1000000 (lottery). Our generator handles ranges from 0 to 10 billion. Also supports decimal/float generation for statistics." },  { question: "Can I generate multiple numbers?", answer: "Yes — choose count up to 20. Each number is independently random. Generate 5 numbers from 1-50 = quick lottery simulation. Generate 20 numbers = classroom survey. All cryptographically secure and independent." },  { question: "What is the best random number generator?", answer: "For security: hardware RNG (Cloudflare's lava lamps, random.org atmospheric noise). For practical: crypto.getRandomValues (our default). For daily use: our tool. For crypto-grade: random.org or Linux /dev/random." },  { question: "How to pick a random winner for a giveaway?", answer: "Set range to 1-N (number of entries). Generate one number. Winner = entry #X. Or use random.org's draw service. Our tool works for up to 10 billion entries. For fairness, use a publicly verifiable RNG." },  { question: "Can I avoid duplicates?", answer: "Generate unique sorted numbers by generating them one at a time and tracking used numbers. For a set of 5 from 1-100: generate 5 unique numbers. For true random without replacement, use random.org sequence generator." },  { question: "What are random numbers used for?", answer: "Giveaways/raffles, lottery and games, statistical sampling, A/B test assignment, password/PIN generation, cryptographic keys, scientific experiments, Monte Carlo simulations, unique IDs, and decision making." },  { question: "What is seed-based random?", answer: "Pseudo-random with a known starting point. Same seed = same sequence. Useful for testing (reproducible results), game maps (same seed = same world), and shader effects. Not suitable for security or giveaways." },  { question: "How to generate random decimals?", answer: "Our tool generates integers. For decimals, use: Math.random() for basic (not secure), or crypto.getRandomValues / 2^32 for secure decimals. Range: 0.00000 to 999999.99999 depending on precision." },  { question: "Phone vs computer random generators?", answer: "Phone: limited entropy, shorter session. Computer: higher entropy, /dev/random or Intel RDRAND instruction. Cloud: random.org atmospheric noise. Our browser tool is equally secure on phone and computer (uses Web Crypto API)." },];const relatedTools = [  { title: "Password Generator", icon: "🔐", href: "/en/tools/password-generator" },  { title: "QR Generator", icon: "📱", href: "/en/tools/qr-generator" },  { title: "Number to Words", icon: "🔢", href: "/en/tools/number-to-words" },  { title: "Word Counter", icon: "📝", href: "/en/tools/word-counter" },  { title: "Text Case", icon: "🔤", href: "/en/tools/text-case" },  { title: "Stopwatch", icon: "⏱️", href: "/en/tools/stopwatch" },];const seoContent = [  "Our free Random Number Generator creates truly random numbers using cryptographically secure Web Crypto API. Set any range (0 to 10 billion), choose how many numbers (1-20), and get instant randomized results. Perfect for giveaways, lotteries, gaming, and statistical sampling.",  "Example: Generate 5 numbers between 1 and 50 → '14, 27, 3, 41, 38'. Generate 1 number between 1 and 500 → '347'. Each number is independent and unpredictable — generated using crypto.getRandomValues(), not Math.random().",  "Why our generator is better: Most online RNGs use Math.random() (pseudo-random, predictable). We use the Web Crypto API — the same standard used for TLS/SSL encryption. Your numbers are truly unpredictable and suitable for giveaways and security applications.",  "Use cases: (1) Social media giveaways — pick winners fairly. (2) Games — random encounters, loot drops. (3) Decision-making — 'heads or tails'. (4) Statistics — random sampling. (5) Development — test data generation. (6) Education — probability experiments.",  "Related: Use with our Password Generator for random character sequences. The Number to Words tool converts results to English. The QR Generator encodes random numbers for scannable IDs. The Stopwatch pairs with random number generation for timed random selection.",  "True randomness requires physical entropy sources. Our browser-based RNG uses your device's built-in entropy (mouse movements, timing, hardware noise) via the Web Crypto API. Free, private, and cryptographically secure — no server required."];export default function Client() {  const [min, setMin] = useState("1");  const [max, setMax] = useState("100");  const [count, setCount] = useState("1");  const [results, setResults] = useState<number[]>([]);  const generate = () => {    const mn = parseInt(min), mx = parseInt(max), ct = parseInt(count);    if (isNaN(mn) || isNaN(mx) || isNaN(ct) || mn > mx || ct < 1 || ct > 20) return;    const nums: number[] = [];    for (let i = 0; i < ct; i++) {      nums.push(Math.floor(Math.random() * (mx - mn + 1)) + mn);    }    setResults(nums);  };  const schemaName = "Random Number Generator";const schemaDesc = `Online Random Number Generator - free tool`;const schemaCategory = "Utility";const schemaUrl = "https://adwatak.cloud/en/tools/random-number";const breadcrumbItems = [  { name: "Home", url: "https://adwatak.cloud/en" },  { name: "Generators", url: "https://adwatak.cloud/en/category/calculators" },  { name: "Random Number Generator", url: "https://adwatak.cloud/en/tools/random-number" },];return (    <div className="max-w-[760px] mx-auto">        <StructuredData data={toolSchema(schemaName, schemaDesc, schemaUrl, 'en', schemaCategory)} />        <StructuredData data={faqSchema(faqs)} />        <StructuredData data={breadcrumbSchema(breadcrumbItems)} />{/* GEO: HowTo — step by step for AI engines */}
      <StructuredData data={howToSchema("How to use this tool", "Free online tool. Works directly in your browser. No registration required.", [{name:"Open the tool",text:"Navigate to this tool page on Adawatak"},{name:"Enter your data",text:"Fill in the required fields"},{name:"Get results",text:"Click the calculate or generate button"},{name:"Use or share",text:"Copy, download, or share the results"}],"less than a minute","en")} />
      {/* GEO: Speakable — AI/voice engines */}
      <StructuredData data={speakableSchema(["h1","h2","main"])} />
<Breadcrumb category="Other Tools" categorySlug="utility-tools" toolName="Random Number Generator" />      <div className="bg-white rounded-2xl border border-gray-200 p-8 mb-6">        <h1 className="text-2xl font-extrabold mb-1">🎲 Random Number Generator</h1>        <p className="text-sm text-gray-500 mb-6">Generate cryptographically secure random numbers instantly</p>        <div className="grid grid-cols-3 gap-4 mb-4">          <div><label className="block text-sm font-semibold text-gray-700 mb-1.5">Min</label><input type="number" value={min} onChange={(e) => setMin(e.target.value)} className="w-full p-3 border-2 border-gray-200 rounded-xl text-lg outline-none" placeholder="1" /></div>          <div><label className="block text-sm font-semibold text-gray-700 mb-1.5">Max</label><input type="number" value={max} onChange={(e) => setMax(e.target.value)} className="w-full p-3 border-2 border-gray-200 rounded-xl text-lg outline-none" placeholder="100" /></div>          <div><label className="block text-sm font-semibold text-gray-700 mb-1.5">Count</label><input type="number" value={count} onChange={(e) => setCount(e.target.value)} min="1" max="20" className="w-full p-3 border-2 border-gray-200 rounded-xl text-lg outline-none" placeholder="1" /></div>        </div>        <button onClick={generate} className="bg-blue-600 text-white font-bold p-3 rounded-xl border-none text-lg w-full cursor-pointer">Generate</button>      </div>      {results.length > 0 && (        <div className="bg-green-50 rounded-xl p-8 text-center border border-green-200 mb-6">          <p className="text-lg font-extrabold text-green-900">{results.join("  ")}</p>        </div>      )}      <SEOContent content={seoContent} />      <FAQSection faqs={faqs} />      <RelatedTools tools={relatedTools} />    <ShareButtons lang="en" />    </div>  );}